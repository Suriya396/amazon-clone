apiVersion: v1
kind: Service
metadata:
  name: {{ include "amazon.fullname" . }}
  labels:
    {{- include "amazon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "amazon.selectorLabels" . | nindent 4 }}
