apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: amazon-staging
  namespace: argocd

spec:
  project: default

  source:
    repoURL: git@github.com:Suriya396/amazon-clone.git
    targetRevision: staging
    path: amazon
    helm:
      valueFiles:
        - ../env/staging/values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: staging

  syncPolicy:
    automated:
      prune: true
      selfHeal: true